<%- include('../partials/layout-top') %>

<div class="h-full flex flex-col -m-6">
  <div class="flex items-center justify-between px-6 py-3 bg-surface-800 border-b border-surface-600">
    <h2 class="text-sm font-semibold text-gray-300">Terminal</h2>
  </div>
  <div id="terminal-full" class="flex-1 bg-surface-900 p-1"></div>
</div>

<%- include('../partials/layout-bottom') %>
